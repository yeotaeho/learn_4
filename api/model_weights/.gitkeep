# 로컬 LLM 모델 파일을 이 디렉토리에 배치하세요.
#
# 예시 구조:
# model_weights/
# ├── my-local-model/
# │   ├── config.json
# │   ├── model.safetensors
# │   ├── tokenizer.json
# │   └── ...
# └── another-model/
#     └── ...
#
# 사용법:
# config = LocalModelConfig(model_path="./model_weights/my-local-model")
# llm = LLMFactory.create(config)

