name: Deploy FastAPI to EC2

on:
  push:
    branches:
      - master
    paths:
      - 'api/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup SSH
      uses: webfactory/ssh-agent@v0.8.0
      with:
        ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

    - name: Add EC2 to known hosts
      working-directory: ${{ github.workspace }}
      env:
        EC2_HOST: ${{ secrets.EC2_HOST }}
      run: |
        echo "🔐 Adding EC2 host to known_hosts..."
        
        # EC2_HOST secret 검증
        if [ -z "$EC2_HOST" ]; then
          echo "❌ ERROR: EC2_HOST secret is empty or not set!"
          echo "Please set EC2_HOST secret in GitHub repository settings."
          exit 1
        fi
        
        mkdir -p $HOME/.ssh
        chmod 700 $HOME/.ssh

        # EC2_HOST에서 포트 번호 제거 (호스트만 추출)
        HOST_ONLY=$(echo "$EC2_HOST" | cut -d: -f1 | xargs)
        
        # 호스트명 검증
        if [ -z "$HOST_ONLY" ]; then
          echo "❌ ERROR: Could not extract hostname from EC2_HOST!"
          echo "EC2_HOST value: '$EC2_HOST'"
          exit 1
        fi
        
        echo "Extracted hostname: $HOST_ONLY"

        # 에러가 발생해도 계속 진행하도록 설정
        set +e

        # ssh-keyscan 실행 (타임아웃 10초)
        ssh-keyscan -T 10 -H "$HOST_ONLY" >> $HOME/.ssh/known_hosts 2>&1
        SSH_KEYSCAN_EXIT=$?

        if [ $SSH_KEYSCAN_EXIT -ne 0 ]; then
          echo "⚠️ ssh-keyscan with -H failed (exit code: $SSH_KEYSCAN_EXIT), trying without -H..."
          ssh-keyscan -T 10 "$HOST_ONLY" >> $HOME/.ssh/known_hosts 2>&1
          SSH_KEYSCAN_EXIT2=$?

          if [ $SSH_KEYSCAN_EXIT2 -ne 0 ]; then
            echo "⚠️ ssh-keyscan failed (exit code: $SSH_KEYSCAN_EXIT2), but continuing..."
            echo "SSH will use StrictHostKeyChecking=accept-new in subsequent steps"
          else
            echo "✅ ssh-keyscan succeeded without -H flag"
          fi
        else
          echo "✅ ssh-keyscan succeeded"
        fi

        # 에러 처리 다시 활성화
        set -e

        # known_hosts 파일이 있으면 권한 설정
        if [ -f $HOME/.ssh/known_hosts ]; then
          chmod 600 $HOME/.ssh/known_hosts
          echo "📋 Known hosts file contents:"
          cat $HOME/.ssh/known_hosts
        else
          echo "⚠️ known_hosts file was not created, but continuing..."
        fi

    - name: Test SSH connection
      working-directory: ${{ github.workspace }}
      env:
        EC2_USER: ${{ secrets.EC2_USER }}
        EC2_HOST: ${{ secrets.EC2_HOST }}
      run: |
        echo "🔍 Testing SSH connection to EC2..."
        
        # EC2_HOST secret 검증
        if [ -z "$EC2_HOST" ]; then
          echo "❌ ERROR: EC2_HOST secret is empty or not set!"
          echo "Please set EC2_HOST secret in GitHub repository settings."
          echo "Format: IP address (e.g., 3.39.230.121) or hostname"
          exit 1
        fi
        
        # EC2_USER secret 검증
        if [ -z "$EC2_USER" ]; then
          echo "❌ ERROR: EC2_USER secret is empty or not set!"
          echo "Please set EC2_USER secret in GitHub repository settings."
          echo "Format: ubuntu (for Ubuntu instances)"
          exit 1
        fi
        
        # 호스트명 추출 (포트 번호 제거)
        HOST_ONLY=$(echo "$EC2_HOST" | cut -d: -f1 | xargs)
        
        # 추출된 호스트명 검증
        if [ -z "$HOST_ONLY" ]; then
          echo "❌ ERROR: Could not extract hostname from EC2_HOST!"
          echo "EC2_HOST value: '$EC2_HOST'"
          exit 1
        fi
        
        echo "Host: $HOST_ONLY"
        echo "User: $EC2_USER"
        echo "Full EC2_HOST: $EC2_HOST"

        # 연결 테스트 (타임아웃 10초)
        timeout 10 ssh -o ConnectTimeout=10 -o StrictHostKeyChecking=accept-new \
          -o UserKnownHostsFile=$HOME/.ssh/known_hosts \
          $EC2_USER@$HOST_ONLY "echo 'SSH connection successful'" || {
          echo "❌ SSH connection failed!"
          echo "Possible causes:"
          echo "1. EC2 instance is not running"
          echo "2. Security group doesn't allow SSH from GitHub Actions IP"
          echo "3. EC2_HOST secret is incorrect (current value: '$EC2_HOST')"
          echo "4. Network connectivity issue"
          exit 1
        }
        echo "✅ SSH connection test passed"

    - name: Deploy to EC2
      working-directory: ${{ github.workspace }}/api
      env:
        EC2_USER: ${{ secrets.EC2_USER }}
        EC2_HOST: ${{ secrets.EC2_HOST }}
      run: |
        echo "🚀 Deploying to EC2..."
        HOST_ONLY=$(echo "$EC2_HOST" | cut -d: -f1)
        echo "Deploying to: $EC2_USER@$HOST_ONLY"

        rsync -avz -e "ssh -o ConnectTimeout=30 -o StrictHostKeyChecking=accept-new -o UserKnownHostsFile=$HOME/.ssh/known_hosts" \
          --exclude 'model_weights' --exclude '__pycache__' \
          --exclude '*.pyc' --exclude '.pytest_cache' --exclude 'venv' \
          ./ $EC2_USER@$HOST_ONLY:~/api/ || {
          echo "❌ rsync failed!"
          echo "Check if EC2 instance is running and accessible"
          exit 1
        }
        echo "✅ Files transferred successfully"

    - name: Install dependencies and restart service
      working-directory: ${{ github.workspace }}
      env:
        EC2_USER: ${{ secrets.EC2_USER }}
        EC2_HOST: ${{ secrets.EC2_HOST }}
      run: |
        HOST_ONLY=$(echo "$EC2_HOST" | cut -d: -f1)
        ssh -o ConnectTimeout=30 -o StrictHostKeyChecking=accept-new -o UserKnownHostsFile=$HOME/.ssh/known_hosts \
          $EC2_USER@$HOST_ONLY << 'EOF'
          cd ~/api

          echo "📦 Setting up Python virtual environment..."
          if [ ! -d "venv" ]; then
            python3 -m venv venv
          fi

          echo "📥 Installing dependencies..."
          source venv/bin/activate
          pip install --upgrade pip
          pip install -r requirements.txt

          echo "🔄 Restarting FastAPI service..."
          sudo systemctl restart fastapi.service

          echo "✅ Deployment completed!"
        EOF

    - name: Health Check
      working-directory: ${{ github.workspace }}
      env:
        EC2_HOST: ${{ secrets.EC2_HOST }}
      run: |
        echo "🏥 Running health check..."
        sleep 10
        response=$(curl -s -o /dev/null -w "%{http_code}" http://$EC2_HOST:8000/health || echo "000")
        if [ "$response" = "200" ]; then
          echo "✅ Health check passed (HTTP $response)"
        else
          echo "⚠️ Health check returned HTTP $response"
          echo "Service might still be starting up..."
        fi


